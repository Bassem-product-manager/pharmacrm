# Committed on purpose: NEXT_PUBLIC_API_URL is a PUBLIC value (it ships inside
# client JS regardless) — not a secret. Next.js inlines it at `next build`.
# Production serverless API hosted on Vercel.
NEXT_PUBLIC_API_URL=https://pharmacrm-api.vercel.app/api/v1
